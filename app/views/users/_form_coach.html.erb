<h1>form coach edit</h1>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-6 col-md-offset-3">
      <div class="text-center">
        <%= simple_form_for(@coach) do |f| %>
        <%= f.input :description %>
<!-- # c'est ici qu'on doit aussi faire apparaitre les sports en cocher par exemple -->
        <%= f.submit "Update profile", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
