 <div class="row">
    <div class="col-xs-8 col-xs-offset-2 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4">
        <h1>Edit my profile</h1>
        <%= render "users/form" %>
        <!-- <% if current_user.coach_id.nil? %>
        <!-- et le user est un coach, on complète le formulaire avce des champs supplémentaires notamment description et sport-->
          <!--<%= render 'coaches/form'  %>
        <% end%> -->
    </div>
  </div>
