<div class="card">

  <%= render 'course_info', course: slot.course %>
  <%= render 'slot_info', slot: slot %>

  <div class="course_access">
    <%= link_to "Se désinscrire", course_path(slot.course) %>
  </div>

 </div>
</div>
