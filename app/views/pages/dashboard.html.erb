<!-- DASHBOARD BACKGROUND CONTAINER-->
<div class="container">

  <div class="dashboard-bg-container">

    <!-- AJOUTER UN NOUVEAU GROUPE -->
    <div class="row">
      <div class="col-xs-offset-1 col-sm-12 col-md-12 col-lg-12">
        <!-- add_a_group_button -->
        <div class="add_a_group_button">
          <% if policy(Group).new? %>
            <%= link_to "Ajouter un nouveau groupe", new_group_path, class:"btn green-button" %>
          <% end %>
        </div>
      </div>
    </div>

    <!-- MES PROCHAINES SEANCES -->
    <h2>Mes prochaines séances </h2>
    <div class="row">
      <% current_user.next_slots.each do |slot| %>
        <div class="col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-2 col-lg-3">
          <%= render 'dashboard-slot-card', slot: slot %>
        </div>
      <% end %>
    </div>

    <!-- PRESENTATION DE TOUS MES GROUPES AVEC LES COURS ASSOCIES -->
    <h2>Tous mes cours </h2>
    <% @user.groups.sort.each do |group| %>

    <!-- GROUP-ROW -->
      <div class="group-container b">
        <!-- group-title -->
        <div class="row">
          <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12"></div>
          <div class="group-title">
            <%= group.name%>
          </div>
        </div>
        <!-- group-crud-buttons -->
        <div class="row">
          <div class="col-xs-offset-2">
            <div class="group-crud-buttons">
              <% if policy(group).edit? %>
                <%= link_to "Créer un nouveau cours", new_group_course_path(group), class:"create-button btn btn-primary"  %>
              <% end %>
              <% if policy(group).edit? %>
                <%= link_to "Editer", edit_group_path(group), class:"delete-button btn btn-warning" %>
              <% end %>
              <% if policy(group).destroy? %>
                <%= link_to "Supprimer", group_path(group), method: "delete", class:"delete-button btn btn-danger" %>
              <% end %>
            </div>
          </div>
        </div>
        <div class="row">
          <% group.courses.sort.each do |course| %>
            <div class="col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-2 col-lg-3">
              <!-- Si coach ou group owner => afficher tous les cours -->
              <% if policy(course).update? %>
                <%= render 'dashboard-course-card', course: course %>
              <!-- Sinon, n'afficher que les acours actifs pour les simples membres -->
              <% else %>
                <% if course.active? %>
                  <%= render 'dashboard-course-card', course: course %>
                <% end %>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>

    <% end %>
    <!-- END-GROUP-ROW -->

  </div>
</div>
