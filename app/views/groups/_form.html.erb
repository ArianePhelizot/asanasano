<%= simple_form_for @group do |f| %>
  <%= f.input :name, placeholder: 'Les petites pastèques ? Le Drakar ? Just do it ? A vous de jouer...', label: false %>
  <%= f.button :submit, "Enregistrer" %>
<% end %>
